import Header from "./components/Header"
import Footer from "./components/Footer"


const Cart = () => {
    return (
        <div>
            <Header />

            <div className="test__export">
                {/* {
                    cartItem.map((item) => {
                        <div key={item.id}>
                        hello
                        <h3>{item.name}</h3>
                        <p>{item.price} $</p>
                        </div>
                    })
                } */}
                sadas
            </div>

            <Footer />
        </div>
    )
}

export default Cart
